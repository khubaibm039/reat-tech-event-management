import Navbar from "./Navbar";

const Header = () => {
    return (
        <div className=" h-[100vh] bg-cover bg-bannerImg max-w-7xl mx-auto bg-no-repeat ">
            <Navbar></Navbar>
        </div>
    );
};

export default Header;
